extends layout

block content
  h1= title
    //- a(class='button' id='addBook' href="/addBook") Add Book #{/*what put here?*/}
    h3 Enter information to edit this author:
      form(method="post" action='/author/#{id}/edit')
        //- select(name="author_id")
        //-   option(value=0) (CHOOSE AUTHOR)
        //-   for author in data
        //-     option(value=author.id) #{author.name}
        input(type="text", value="#{name}", name="name")
        input(type="text", value="#{img_url}", name="img_url")
        textarea(rows="4" cols="50", name="bio") #{bio}
        input(type="submit" class='button' value="Update Author")
